## Objective
Rewrite the database models section of `ARCHITECTURE.md` so it only describes Mongoose models that exist under `lib/models/` and accurately states where session and ranking data live.

## Unified Context
`ARCHITECTURE.md` lists **Session**, **PersonalRanking**, **GlobalRanking**, **SystemVersion**, **FontPreset**, **BackgroundPreset**, and **Deck** as models. The repository contains **Play**, **Card**, **Organization**, mode-specific play models (`SwipeOnlyPlay`, `RankMorePlay`, …), **User**, and **PagePassword**.

Session state is embedded in Play and mode collections. Global scores are on **Card** (`globalScore`, `voteCount`, `winCount`). Decks are a logical concept (parent card with children), not a collection.

## Problem
Architecture diagrams drive wrong abstractions—developers search for `Session.js`, build migrations for `GlobalRanking`, or plan theme presets that have no schema.

## Goal
ARCHITECTURE model section matches filesystem; ELO and session semantics point to correct files; removed models listed in `docs/FUTURE.md` if still desired.

## Scope
### In Scope
- Replace model inventory with actual files and one-sentence responsibility each
- Note dual persistence: `Play` vs `SwipeOnlyPlay` / `RankMorePlay` / etc. via `PlayDispatcher`
- Document ELO on Card, not GlobalRanking collection
- Remove or relocate optimistic locking on Session (not implemented—see FUTURE.md)
- Keep valid mermaid flows that still reflect play.js behavior; fix labels that say “Session” when meaning “Play”

### Out of Scope
- Creating missing models unless separate product issues
- Full ARCHITECTURE rewrite (other sections handled in #12)

## Execution Prompt
`ls lib/models`. For each file, read schema summary. Replace ARCHITECTURE § Database Models. Grep ARCHITECTURE for removed model names and fix or footnote.

## Constraints
- Must not reintroduce TypeScript paths
- Cross-link unified doc for field name mapping

## Acceptance Checks
- [ ] Every `lib/models/*.js` (excluding `* 2.js`) mentioned in ARCHITECTURE
- [ ] No reference to Session.js, GlobalRanking.js, FontPreset without FUTURE.md label
- [ ] PlayDispatcher dual-model strategy described in ≤1 paragraph
- [ ] ELO fields documented on Card schema

## Dependencies
- **Follows:** #12 (ARCHITECTURE banner/trim)
- **Related:** #18
- **Parent program:** #11

## Risks
- ARCHITECTURE mermaid diagrams still wrong at flow level—fix only model section in this issue or split follow-up

## Delivery Artifact
PR updating ARCHITECTURE.md model section (+ minor diagram label fixes if trivial).

## Developer Notes
Ignore `lib/models/* 2.js`—see #22.
