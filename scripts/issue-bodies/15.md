## Objective
Restore `WARP.md` as a trustworthy AI-agent onboarding document by removing false patterns and aligning every command, path, and architectural claim with the JavaScript Pages Router codebase.

## Unified Context
`WARP.md` is labeled “Single Source of Truth” for Warp/Cursor agents. It currently instructs agents to import non-existent `UUID_FIELDS`, forbids using `uuid` while models require it, cites `buildOrgMongoUri()` in `lib/db.js` (not implemented), lists Next.js 15.5.3 (package has 15.5.9), and names npm packages not in `package.json` (@react-spring/web, @use-gesture/react).

Agents following WARP will generate broken code and wrong API calls.

## Problem
Bad agent guidance multiplies doc debt faster than human README drift—every session may reintroduce wrong field names and API paths.

## Goal
A repaired `WARP.md` that:
- Points to `narimato_unified_documentation.md` as canonical
- Documents `fieldNames` **alias map** correctly
- Describes actual `connectDB()` single-tenant behavior (pending #17 ADR)
- Lists real dependencies from `package.json`
- Documents correct rate limits and play file paths

## Scope
### In Scope
- Fix Next.js version to **15.5.9**
- Replace `UUID_FIELDS` / ESM import example with CommonJS `fieldNames` usage (see `VoteOnlyService.js`)
- Rewrite “NEVER use uuid” to “use `fieldNames.PlayUUID` when building queries; stored key is `uuid`”
- Remove or gate multi-tenant section behind ADR #17
- Remove animation libs from stack unless added to `package.json`
- Update rate limiting section: start 60/min, input 120/min (`pages/api/v1/play/start.js`, `input.js`)
- Add “Tests: none—use root diagnostic scripts” (keep accurate MVP rule)
- Link to `docs/API_REFERENCE.md` after #13

### Out of Scope
- Rewriting entire WARP governance sections unrelated to accuracy
- Changing agent versioning rules unless team agrees

## Execution Prompt
Diff `WARP.md` against `lib/db.js`, `lib/constants/fieldNames.js`, `package.json`, and `pages/api/v1/play/`. For each Critical Patterns subsection, verify with grep. Delete fiction; add file path citations.

## Constraints
- Must not contradict accepted ADRs #14 and #17
- Keep WARP concise—defer HTTP details to API_REFERENCE

## Acceptance Checks
- [ ] No mention of `UUID_FIELDS`, `buildOrgMongoUri`, or `app/lib/*.ts` in WARP.md
- [ ] Code sample for field names matches `require('../constants/fieldNames')` pattern
- [ ] Next.js version matches `package.json`
- [ ] Dependency list matches `package.json` dependencies block
- [ ] Play API paths match existing files under `pages/api/v1/play/`

## Dependencies
- **Blocked by:** #17 for final multi-tenant wording
- **Should follow:** #12 canonical spec merge
- **Related:** #13 API reference
- **Parent program:** #11

## Risks
- Over-correction removes useful operational notes—preserve accurate governance (version bump rules, script list)

## Delivery Artifact
Merged PR updating `WARP.md` only (or WARP + minimal cross-links in README).

## Developer Notes
`lib/db.js` exports only `connectDB`. Gesture code lives in `lib/utils/useSwipeGestures.js` without @use-gesture/react.
