import { extendGdsTheme } from '@gds/theme';

/**
 * Narimato theme extends @gds/theme (General Design System — external dependency).
 * Product-specific overrides only.
 */
export const narimatoTheme = extendGdsTheme({
  other: {
    appName: 'NARIMATO',
  },
});
