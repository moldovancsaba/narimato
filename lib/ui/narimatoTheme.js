import { extendGdsTheme } from '@doneisbetter/gds-theme/server';

/**
 * Narimato brand extension on the published GDS theme (SSOT: general-design-system).
 */
export const narimatoTheme = extendGdsTheme({
  other: {
    appName: 'NARIMATO',
  },
});
